// Runtime config for static deployments (Render, Netlify, etc).
// Set window.REPORTE_API_BASE_URL to point this frontend to a backend URL.
window.REPORTE_API_BASE_URL = window.REPORTE_API_BASE_URL || '';
